import { useState } from 'react'; import Input from '../common/Input'; import Button from '../common/Button'; import { passwordRules } from '../../utils/validation';
export default function ResetPasswordForm({ onSubmit }) { const [form, setForm] = useState({ current: '', password: '', confirm: '' }); const [error, setError] = useState(''); const update = e => setForm({ ...form, [e.target.name]: e.target.value }); const submit = e => { e.preventDefault(); if (!Object.values(form).every(Boolean)) return setError('Please complete all fields.'); if (!passwordRules(form.password)) return setError('Your new password must meet the listed requirements.'); if (form.password !== form.confirm) return setError('New passwords do not match.'); onSubmit(form); }; return <form className="content-form" onSubmit={submit}><Input label="Current password" name="current" type="password" autoComplete="current-password" value={form.current} onChange={update} /><Input label="New password" name="password" type="password" autoComplete="new-password" value={form.password} onChange={update} /><Input label="Confirm new password" name="confirm" type="password" autoComplete="new-password" value={form.confirm} onChange={update} />{error && <p className="form-error">{error}</p>}<Button>Update password</Button></form>; }
