import { CalendarDays, Flame, Link2, MonitorCog, Plus, Target } from 'lucide-react'; import { Link } from 'react-router-dom'; import { useEffect, useState } from 'react'; import AdminShell from '../components/layout/AdminShell'; import LoadingSpinner from '../components/common/LoadingSpinner'; import { useAuth } from '../context/AuthContext'; import { linkService } from '../services/linkService';
const config = [{ label: 'Total Links', category: null, icon: Link2, tone: 'purple' }, { label: 'Contests', category: 'Contest', icon: Flame, tone: 'orange' }, { label: 'Daily Labs', category: 'Daily Lab', icon: MonitorCog, tone: 'blue' }, { label: 'Single Problems', category: 'Single Problem', icon: Target, tone: 'pink' }];
export default function AdminDashboard() { const { currentUser } = useAuth(); const [links, setLinks] = useState(null); useEffect(() => { linkService.getMyLinks(currentUser.labId).then(setLinks); }, [currentUser.labId]); return <AdminShell title="Dashboard" action={<Link className="button button--primary header-action" to="/admin/add-link"><Plus size={17} />Add link</Link>}><section className="dashboard-content"><div className="dashboard-welcome"><div><p className="eyebrow">My lab</p><h2>{currentUser.labName}</h2><p>Here’s a quick picture of your current resources.</p></div><span><CalendarDays size={18} /> 19 Aug 2026</span></div>{!links ? <LoadingSpinner label="Loading your dashboard..." /> : <><div className="stats-grid">{config.map(({ label, category, icon: Icon, tone }) => <article className="stat-card" key={label}><span className={`stat-icon stat-icon--${tone}`}><Icon /></span><p>{label}</p><h3>{category ? links.filter(link => link.category === category).length : links.length}</h3></article>)}</div><section className="dashboard-panel"><div className="panel-title"><div><p className="eyebrow">Recent activity</p><h2>Latest links</h2></div><Link to="/admin/manage-links">Manage all</Link></div><div className="recent-list">{links.slice(0, 4).map(link => <div key={link.id}><span className="recent-bullet" /><div><b>{link.title}</b><small>{link.category} · {link.date}</small></div><span className="badge">{link.status}</span></div>)}</div></section></>}</section></AdminShell>; }
