import { Pencil, Plus, Trash2 } from 'lucide-react'; import { useEffect, useState } from 'react'; import { Link } from 'react-router-dom'; import AdminShell from '../components/layout/AdminShell'; import Modal from '../components/common/Modal'; import LoadingSpinner from '../components/common/LoadingSpinner'; import AddLinkForm from '../components/admin/AddLinkForm'; import { linkService } from '../services/linkService'; import { useAuth } from '../context/AuthContext';
export default function ManageLinks() { const { currentUser } = useAuth(); const [links, setLinks] = useState(null), [selected, setSelected] = useState(null), [editing, setEditing] = useState(null); const load = () => linkService.getMyLinks(currentUser.labId).then(setLinks); useEffect(() => { load(); }, [currentUser.labId]); const remove = async () => { await linkService.deleteLink(selected.id); setSelected(null); load(); }; const update = async form => { await linkService.updateLink(editing.id, form); setEditing(null); load(); }; return <AdminShell title="Manage Links" action={<Link className="button button--primary header-action" to="/admin/add-link"><Plus size={17} />Add link</Link>}><section className="dashboard-content"><div className="page-heading"><p className="eyebrow">Your lab resources</p><h2>Manage links</h2><p>Edit or remove resources from {currentUser.labName}.</p></div>{!links ? <LoadingSpinner /> : <div className="table-wrap"><table><thead><tr><th>Title</th><th>Category</th><th>Date</th><th>Status</th><th aria-label="Actions" /></tr></thead><tbody>{links.map(link => <tr key={link.id}><td><b>{link.title}</b><small>{link.description}</small></td><td><span className="category-pill">{link.category}</span></td><td>{link.date}</td><td><span className="badge">{link.status}</span></td><td><div className="table-actions"><button className="icon-button" title="Edit link" onClick={() => setEditing(link)}><Pencil size={16} /></button><button className="icon-button danger" title="Delete link" onClick={() => setSelected(link)}><Trash2 size={16} /></button></div></td></tr>)}</tbody></table>{!links.length && <div className="empty-state">No links yet. <Link to="/admin/add-link">Add your first one.</Link></div>}</div>}</section>{selected && <Modal title="Delete this link?" onClose={() => setSelected(null)}><p>Are you sure you want to delete <b>{selected.title}</b>? This action cannot be undone.</p><div className="modal-actions"><button className="button button--secondary" onClick={() => setSelected(null)}>Cancel</button><button className="button button--danger" onClick={remove}>Delete link</button></div></Modal>}{editing && <Modal title="Edit link" onClose={() => setEditing(null)}><AddLinkForm initialValue={editing} onSubmit={update} /></Modal>}</AdminShell>; }
